
**Course Description:** Notes and commands for the YSAP Bash Scripting course.
## Table of Contents

* [[terminal_and_finder]] - Intro to the BASH REPL, directory context, and basic navigation (`pwd`, `ls`, `cd`, `touch`, `rm`).
* [[basic_file_manipulation]] - Moving, renaming, and removing files safely (`mv`, `rm -i`, `alias`, `history`).
* [[hidden_files]] - Viewing hidden files and directory shortcut navigation (`ls -a`, `.`, `..`, `cd -`).
* [[searching_files]] - Reading files, filtering string content with Regex, and piping/redirecting output (`cat`, `grep`, `>`, `|`).
* [[paging_files]] - Viewing large files interactively in a pager environment (`less`, `more`).
* [[man_pages]] - Accessing built-in shell command manuals, documentation, and file types (`man`, `help`, `type`, `compgen`, `file`, `tr`).

---
**Course Progress:** #in-progress #course
